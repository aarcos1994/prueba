name: HolaMundo
on:
  workflow_dispatch
jobs: 
  mi-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: mi action con actions
        uses: actions/hello-world-javascript-action@main
        with:
         who-to-greet: 'Les saluda Armando'
